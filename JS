import http from 'http';

const PORT = process.env.PORT || 3000;

http.createServer((req, res) => {
  if (req.url === '/health') {
    res.writeHead(200, { 'Content-Type': 'text/plain' });
    return res.end('OK');
  }

  res.writeHead(200, { 'Content-Type': 'text/plain' });
  res.end('Fantasy Town Bot is running');
}).listen(PORT, () => {
  console.log(`🌐 Web server listening on port ${PORT}`);
});
